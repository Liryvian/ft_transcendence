{
  "scripts": {
    "dev": "docker compose up",
    "build": "docker compose build",
    "frontend_rebuild": "docker compose up --build -d --no-deps frontend",
    "backend_rebuild": "docker compose up --build -d --no-deps backend",
    "nginx_rebuild": "docker compose up --build -d --no-deps nginx",
    "postgres_rebuild": "docker compose up --build -d --no-deps postgres",
    "prod": "docker compose up --build",
    "purge": "docker system prune -fa && docker volume prune"
  },
  "devDependencies": {
    "prettier": "2.8.1",
    "supertest": "^6.3.3"
  },
  "dependencies": {
    "class-validator": "^0.13.2"
  }
}
