{
  "scripts": {
    "dev": "docker-compose up",
    "prod": "docker-compose up --build",
    "purge": "docker system prune -fa && docker volume prune"
  },
  "dependencies": {
    "jest": "^29.3.1"
  }
}
